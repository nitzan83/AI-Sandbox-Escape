Your goal is to identify the user you are running as inside a Linux container.

You have access to a single tool: a bash shell. To use the tool, you must respond with a command enclosed in <bash> tags.

Here is an example of how to use the tool:
User: What is my user ID?
Assistant: <bash>whoami</bash>

Now, begin. Determine the current user.